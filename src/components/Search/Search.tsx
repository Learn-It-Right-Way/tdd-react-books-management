import React from "react";

const Search = () => {
    return (
        <div>
            <input type="text" placeholder="Search" data-test="input-search" />
            <button>Search</button>
        </div>
    );
};

export default Search;
